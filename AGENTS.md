# 仓库级 Agent 指令

本仓库后续新增或更新 `skills`、`plugins`、Agent 工具链、AI 辅助项目说明文档时，默认遵循下面这些写法偏好。

## 1. 总体风格

- 以中文写作，专业、直接、少废话。
- 优先写成“可直接使用的说明文档”，不要写成长篇项目介绍或宣传稿。
- 先给读者最重要的信息：这是什么、适合谁、怎么装、怎么用。
- 避免大段背景铺垫；背景只写到能帮助理解为止。
- 如果用户没有要求，不要展开太多底层实现、内部架构或编程细节。

## 2. 插件 / skill / Agent 项目文档

这类文档优先采用下面的顺序：

1. 开头一句话说明项目是什么
2. 如果有多个仓库，先说明分别对应哪个 Agent / 平台
3. 给出项目地址
4. 说明它如何影响 Agent 的工作方式
5. 分平台写安装步骤
6. 给少量高质量通用提示词

默认不要：

- 写很长的能力总览
- 堆太多命令、模式、开关
- 把 README 的所有内容都搬过来
- 过度偏向某一个 Agent，除非用户明确要求

## 3. 多仓库项目的写法

如果一个项目同时维护多个仓库：

- 先做中立总览
- 明确每个仓库分别服务哪个平台或 Agent
- 不把它们写成“主次争夺”关系，除非官方文档明确这样定义
- 对安装、入口、命令分别写清楚，不混在一起

如果用户明确说“不要偏向某一个 Agent”，就：

- 不用某一端的术语主导全文
- 不把某一端当默认读者
- 用“分别安装、分别调用、通用提示词”这种结构

## 4. 提示词示例的写法

- 数量少，质量高。
- 提示词优先覆盖最常见任务，不追求面面俱到。
- 一条提示词只解决一个明确阶段的问题。
- 优先写“收敛问题、规划结构、自查审稿、回复意见”这类高价值任务。
- 不要堆很多相似提示词。
- 默认强调先明确阶段，再要求输出。

## 5. graphify 类项目的写法

如果是像 `graphify` 这种工具型项目：

- 尽量保留原有教程框架，除非用户要求重构
- 重点核对旧仓库名、旧平台列表、旧安装命令是否过时
- 统一改成当前官方口径
- 明确区分：系统命令、对话命令、平台常驻安装
- 对 `Codex`、`Claude Code`、`OpenCode` 这类平台分别写清执行位置

## 6. academic / research 类项目的写法

- 不写成大而泛的研究方法论文式介绍
- 聚焦：这是什么、给谁用、怎么装、怎么驱动 Agent
- 如果用户要求精简，就只保留：
  - 项目介绍
  - 仓库对应关系
  - 安装
  - 通用提示词

## 7. 更新旧文档时

- 优先核对官方仓库当前 README / CHANGELOG / 安装文档
- 如果旧文档时间较早，要显式修正：
  - 仓库地址
  - 平台支持列表
  - 当前推荐安装命令
  - 当前推荐入口命令
- 能保留原框架时尽量保留，但内容应更新到当前口径

## 8. Git 相关

- 这类文档更新如果已经可用，默认可以纳入一次正常提交。
- 提交时优先只带当前任务相关文件。
- 如果用户明确要求“上传”，则在提交后推送远端。

## 9. 正文与附录

- 正文应保持安装、配置和使用流程连续。读者按顺序操作时，不应被长篇背景或实验说明打断。
- 背景、设计依据、实验方法、完整数据、可靠性说明和适用范围较长时，移到文末附录。
- 正文只保留理解或操作所需的简短摘要，并用清晰的相对链接指向附录。
- 不要为了缩短正文删除验证证据；调整信息层级和位置。
- 安装前提、必要命令、安全警告以及直接影响操作结果的限制仍放在正文。
- 新增内容时先判断读者是否必须在当前步骤知道：必须知道就留在当前位置，否则优先放入附录。

## 10. Markdown 操作步骤与排版

- 非代码的说明、参数和界面操作默认写成简洁段落，不使用 `text` 代码块；只有用户可直接执行的命令、配置片段或代码保留代码块。
- 对需要严格按顺序执行的操作，尤其是用户手动调整为列表的章节，保留其编号列表和层级，不应仅为压缩页面而改写成段落。
- 用户手动修改过的文档章节应视为该章节的排版与表达偏好；除非用户明确要求，否则更新其他内容时不得改变其结构或遗漏其中的信息。

## 11. 跨平台操作与排障文档

- 跨 Windows、Linux、macOS 的操作指南，先给出统一流程，再只在实际操作、路径、权限或平台功能有差异处拆分系统说明；仅变量引用或一两条命令不同的场景，优先合并到同一代码块，并用系统注释标明。
- 文档开头若需要读者反复替换参数，使用“占位符名字 / 示例 / 说明”表格，并提供各系统可直接执行的变量设置块。相同概念在 PowerShell 和 shell 中使用同名变量；后续命令应引用变量，而非重新散落硬编码值。说明变量的作用域与有效期。
- 命令必须可安全重复理解：涉及密钥、配置和权限时，明确是否会覆盖、追加或保留原内容；在命令前紧邻提示同名文件、重复配置、权限收紧、主机指纹确认及私钥保密等直接风险。
- 解释排障时先区分故障阶段（本地变量或配置、DNS/TCP 连通性、SSH 身份认证、仓库授权），避免将网络瞬断或其他前置错误误写为凭据尚未生效。对可恢复的短暂现象说明重试条件和持续失败时的检查入口。
- 正文保留首次配置所需的连续步骤、关键解释与安全提醒；面向熟悉流程的整块快速部署命令放入附录，并在开头提供清晰的章节链接。附录应包含必要前提、人工网页操作提示和最小验证，但不取代正文说明。
- 对 Windows 权限、服务和文件操作，优先使用 PowerShell 原生命令或已验证的 API；只有在确有兼容性问题时才引入替代方案，并说明适用条件。
- 引用官方文档时，将链接放在相关结论附近。展示命令输出、账户、路径或报错时，删除用户名、邮箱、仓库名、主机内网地址、令牌和私钥等敏感信息。
- 操作文档提交前至少检查：代码块成对闭合、参考链接定义完整、`git diff --check` 通过，以及新增示例不含敏感信息。

## 12. 文档偏好维护

- 每次完成其他文档的实质性改进后，复盘用户明确提出或手动确认的写作、排版、命令组织与验证偏好。
- 若某项偏好可复用于多个文档，应及时以通用、可执行的规则更新本 `AGENTS.md`；一次性项目背景、具体账户、固定命令或仅适用于单篇文章的细节不写入全局指令。
